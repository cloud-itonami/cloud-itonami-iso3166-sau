(ns statute.facts-test
  (:require [clojure.string :as str]
            [clojure.test :refer [deftest is]]
            [statute.facts :as facts]))

(deftest sau-has-spec-basis
  (let [sb (facts/spec-basis "SAU")]
    (is (= 2 (count sb)))
    (is (every? #(str/starts-with? (:statute/url %) "https://") sb))
    (is (every? :statute/law-number sb))))

(deftest unknown-jurisdiction-has-no-spec-basis
  (is (nil? (facts/spec-basis "ATL")))
  (is (nil? (facts/spec-basis "ZZZ"))))

(deftest coverage-is-honest
  (let [c (facts/coverage ["SAU" "JPN" "ATL"])]
    (is (= 3 (:requested c)))
    (is (= 1 (:covered c)))
    (is (= ["ATL" "JPN"] (:missing-jurisdictions c)))))

(deftest by-topic-filters
  (is (= ["sau.royal-decree-m19-1443-personal-data-protection-law"]
         (mapv :statute/id (facts/by-topic "SAU" :privacy))))
  (is (empty? (facts/by-topic "SAU" :labor)))
  (is (empty? (facts/by-topic "ATL" :privacy))))
